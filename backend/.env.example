# MongoDB connection string (replace placeholders)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.example.mongodb.net/<dbname>?retryWrites=true&w=majority

# Server port (optional)
PORT=5000
